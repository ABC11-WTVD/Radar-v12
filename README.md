# Radar-v12

Radar is a mobile-first personalized interest tracking app with the tagline:

> Never Miss What Matters.

## Quick preview

Open `preview.html` directly in a browser to preview the current Radar prototype without running a build step.

For local development of the React app:

```bash
npm install
npm run dev
```
